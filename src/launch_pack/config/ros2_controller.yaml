controller_manager:
  ros__parameters:
    update_rate: 500

    arm_sim:
      type: arm_simulate/Arm_sim


arm_sim:
  ros__parameters:

    joints:
      - joint1
      - joint2
      - joint3
      - joint4

    command_interfaces:
      - position

    state_interfaces:
      - position